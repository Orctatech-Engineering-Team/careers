{
  "name": "orcta-careers",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "generate-og": "node --experimental-strip-types -e \"import sharp from 'sharp'; import {readFileSync} from 'fs'; const svg=readFileSync('public/og-image.svg'); await sharp(svg).resize(1200,630).png().toFile('public/og-image.png'); console.log('OG PNG generated');\"",
    "build": "npm run generate-og && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.3.7"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}